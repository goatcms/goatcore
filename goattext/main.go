package goattext

const (
	// FileExtension is default template file extension
	FileExtension = ".gotext"
	// DefaultLayout is default layout name
	DefaultLayout = "default"
	// LayoutPath is default layout path pattern ("{name}" is replace with layout name)
	LayoutPath = "layouts/{name}/"
	// ViewPath is default view path pattern ("{name}" is replace with template name)
	ViewPath = "views/{name}/"
	// HelpersPath is default helpers path
	HelpersPath = "helpers/"
)
