{{- define "postgres" -}}
{{- $host := (index .Properties.Secrets "test.postgres.host") -}}
{{- $username := (index .Properties.Secrets "test.postgres.username") -}}
{{- $password := (index .Properties.Secrets "test.postgres.password") -}}
{{- $database := (index .Properties.Secrets "test.postgres.database") -}}

{
  "url":"postgres://{{$username}}:{{$password}}@{{$host}}/{{$database}}?sslmode=disable"
}
{{- end -}}
