{{- define "smtp" -}}
{
  "sender": {
    "smtpAddr": "{{index .Properties.Secrets "test.smtp.url"}}",
    "authUsername": "{{index .Properties.Secrets "test.smtp.username"}}",
    "authPassword": "{{index .Properties.Secrets "test.smtp.password"}}",
    "authIdentity": "{{index .Properties.Secrets "test.smtp.identity"}}"
  },
  "fromAddress": "{{index .Properties.Secrets "test.smtp.fromAddress"}}",
  "toAddress": "{{index .Properties.Secrets "test.smtp.toAddress"}}"
}
{{- end -}}
