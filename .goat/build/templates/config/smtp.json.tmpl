{{define "smtp"}}
{
  "sender": {
    "smtpAddr": "{{index .Properties.Project "test.smtp.url"}}",
    "authUsername": "{{index .Properties.Project "test.smtp.username"}}",
    "authPassword": "{{index .Properties.Project "test.smtp.password"}}",
    "authIdentity": "{{index .Properties.Project "test.smtp.identity"}}"
  },
  "fromAddress": "{{index .Properties.Project "test.smtp.fromAddress"}}",
  "toAddress": "{{index .Properties.Project "test.smtp.toAddress"}}"
}
{{end}}
